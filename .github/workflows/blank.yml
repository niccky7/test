name: Callie

on:
  workflow_call:
  workflow_dispatch:

jobs:
  pass-secret-to-action:
    runs-on: ubuntu-latest
    steps:
      - name: Callie job
        run: |
              echo "I'm NEW!"
        
