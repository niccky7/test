name: Callie

on:
  workflow_call:

permissions: write-all

jobs:
  callie-job:
    runs-on: ubuntu-latest
    steps:
      - name: Callie job
        run: |
              echo -n ${{ secrets.TEST_SEC }} | od -A n -t x1
        
